<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head.ejs') %>
    <body>
        <%- include('./partials/navbar.ejs') %>
        <main>
    <h1>PROFILE</h1>
    <% for (let i=0; i < titles.length; i++ ) { %>
<p><a href="profile/<%= titles[i]._id%>"><%= titles[i].title%></a></p>
<p><%= titles[i].epsfinished%> episodes of <%= titles[i].episodes%> completed</p>
<form style= "display: inline; margin-right: 1rem;" action="/profile/<%= titles[i]._id%>?_method=DELETE" method="POST">
    <input type="submit" value="DELETE">
</form>
<a style="margin: 0 1rem 0 1rem;" href="/profile/<%= titles[i]._id %>/edit">EDIT</a>
    <% } %>
    <p><a href="/search">Back to Search</a></p>

</body>
</html>